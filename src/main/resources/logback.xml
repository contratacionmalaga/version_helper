<configuration scan="true" scanPeriod="30 seconds">

    <!-- ===================== -->
    <!-- PROPIEDADES -->
    <!-- ===================== -->

    <property name="APP_NAME" value="version-helper"/>
    <property name="LOG_PATH" value="logs"/>

    <property name="LOG_PATTERN"
              value="%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%file:%line] - %msg%n"/>

    <!-- ===================== -->
    <!-- APPENDER CONSOLA -->
    <!-- ===================== -->

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>${LOG_PATTERN}</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!-- ===================== -->
    <!-- APPENDER FICHERO -->
    <!-- ===================== -->

    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_PATH}/${APP_NAME}.log</file>

        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>
                ${LOG_PATH}/${APP_NAME}.%d{yyyy-MM-dd}.%i.log
            </fileNamePattern>
            <maxFileSize>10MB</maxFileSize>
            <maxHistory>30</maxHistory>
            <totalSizeCap>1GB</totalSizeCap>
        </rollingPolicy>

        <encoder>
            <pattern>${LOG_PATTERN}</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!-- ===================== -->
    <!-- LOGGERS DE LA APP -->
    <!-- ===================== -->

    <!-- Flujo principal -->
    <logger name="local.jarios.version" level="info" additivity="false">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
    </logger>

    <!-- Detalle técnico (solo si subes nivel a DEBUG) -->
    <logger name="local.jarios.version.api" level="info"/>
    <logger name="local.jarios.version.helpers" level="info"/>

    <!-- ===================== -->
    <!-- ROOT -->
    <!-- ===================== -->

    <root level="warn">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
    </root>

</configuration>
